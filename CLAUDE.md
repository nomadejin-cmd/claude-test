# CLAUDE.md

이 파일은 Claude Code (claude.ai/code)가 이 저장소에서 작업할 때 참고할 가이드를 제공합니다.

## 프로젝트 개요

영수증 이미지를 업로드하면 OCR(광학 문자 인식)을 통해 전표번호, 고객명, 전화번호, 주소를 자동으로 추출하고 수정할 수 있는 웹 애플리케이션입니다. 다크 테마의 AI 스타일 디자인으로 제작되었습니다.

## 아키텍처

**단일 파일 구조**: 모든 HTML, CSS, JavaScript가 `index.html`에 포함되어 있습니다.

### 핵심 기술 스택
- **Tesseract.js**: 브라우저 기반 OCR 라이브러리 (한글+영문 지원)
- **HTML5 File API**: 이미지 업로드 및 미리보기
- **LocalStorage**: 추출된 데이터 저장

### HTML 구조
- **이미지 업로드 패널**:
  - 드래그 앤 드롭 지원 업로드 영역
  - 이미지 미리보기
  - 분석 진행률 표시 (스피너 + 퍼센트)
  - 분석하기 버튼

- **추출 정보 패널**:
  - 전표번호 입력 필드
  - 고객명 입력 필드
  - 전화번호 입력 필드
  - 주소 입력 필드
  - 저장/초기화 버튼
  - 저장된 데이터 표시 영역

### CSS 스타일
- **다크 테마**: 네이비/블랙 계열 그라데이션 배경 (`#0a0e27`, `#1a1a2e`, `#16213e`)
- **네온 효과**:
  - 제목: Cyan 글로우 (`#00ffff`)
  - 패널 제목: Magenta 글로우 (`#ff00ff`)
- **애니메이션**:
  - `gradientShift`: 배경 그라데이션 이동 (15초)
  - `glow`: 텍스트 글로우 효과 (3초)
  - `spin`: 로딩 스피너 회전
- **반응형 디자인**: 968px 브레이크포인트 (모바일에서 1열 레이아웃)

### JavaScript 주요 기능

#### 1. 이미지 업로드
- 클릭 업로드 및 드래그 앤 드롭 지원
- 이미지 파일 유효성 검사
- 실시간 미리보기

#### 2. OCR 처리
- **Tesseract.js 사용**: `kor+eng` 언어 모델
- 비동기 처리로 UI 블로킹 방지
- 진행률 실시간 업데이트

#### 3. 정보 추출 로직
정규식 패턴을 사용한 데이터 추출 (우선순위 순):

**전표번호:** (숫자만 추출)
- 키워드: `배달번호`, `배달전표`, `전표번호`, `번호` 뒤의 값
- 예시: `배달번호: 1-(09:20)` → `10920` (숫자만 추출)
- 모든 특수문자(`-`, `(`, `)`, `:`) 제거 후 숫자만 저장
- 패턴:
  1. `/(?:배달번호|배달전표|전표번호|번호)[:\s]*([0-9-():]+)/i`
  2. `/(?:배달|전표)[:\s]*[*\s]*[:\s]*([0-9-():]+)/i`

**전화번호:** (하이픈 자동 정규화)
1. `/H\.P[:\s.]*([0-9-]{10,13})/i` - H.P 패턴 (점 포함)
2. `/HP[:\s.]*([0-9-]{10,13})/i` - HP 패턴
3. `/(?:전화|TEL)[:\s]*([0-9-]{10,13})/i` - 전화/TEL 패턴
4. `/(010[-\s]?\d{3,4}[-\s]?\d{4})/` - 010 패턴
5. `/(\d{2,3}[-\s]\d{3,4}[-\s]\d{4})/` - 일반 전화번호 패턴
- 추출 후 `010-XXXX-XXXX` 형식으로 정규화

**고객명:** (`성명:` 값, 마스킹 `*` 포함)
- 예시: `성명: 강**` → `강**`
- 패턴:
  1. `/성명[:\s]*([가-힣*\s]+?)(?=\n|$|H\.P|HP|전화|TEL|주소)/i` - 다음 키워드 전까지
  2. `/이름[:\s]*([가-힣*]{2,10})/` - 이름 키워드
  3. `/고객[:\s]*([가-힣*]{2,10})/` - 고객 키워드

**주소:** (`주소:` 값, 여러 줄 지원)
- 줄바꿈을 공백으로 변환, 중복 공백 제거
- 패턴:
  1. `/주소[:\s]*(.+?)(?=\n.*(?:TEL|H\.P|HP|전화|비고|배달번호|배달전표|메모|$))/is` - 다음 키워드 전까지
  2. `/주소[:\s]*(.+?)(?=\*배달)/is` - *배달 전까지
  3. `/주소[:\s]*([^\n]+)/i` - 한 줄 주소
```

#### 4. 데이터 저장
- LocalStorage 사용
- JSON 형식으로 저장
- 페이지 새로고침 시 자동 복원

## 실행 방법

`index.html` 파일을 웹 브라우저에서 직접 열면 됩니다. 인터넷 연결 필요 (Tesseract.js CDN 로드).

## 사용 방법

1. 영수증 이미지를 업로드 영역에 드래그하거나 클릭하여 선택
2. "분석하기" 버튼 클릭 (OCR 처리 시작)
3. 추출된 정보가 입력 필드에 자동으로 표시됨
4. 필요시 수정 가능
5. "저장하기" 버튼으로 데이터 저장
6. "초기화" 버튼으로 모든 데이터 리셋

## 수정 방법

### OCR 언어 변경
`index.html:450-452` - Tesseract 언어 설정:
```javascript
const result = await Tesseract.recognize(
    currentImageFile,
    'kor+eng',  // 한글+영문 (jpn, chi_sim 등으로 변경 가능)
    ...
)
```

### 정보 추출 패턴 수정
`index.html:480-525` - `extractInformation()` 함수 내 정규식 패턴 수정

### 추출 필드 추가
1. HTML에 새로운 `.form-group` 추가
2. `extractInformation()` 함수에 추출 로직 추가
3. `saveBtn` 이벤트 리스너에 저장 로직 추가

### 색상 테마 변경
주요 색상 변수:
- Cyan: `#00ffff` (주 강조색)
- Magenta: `#ff00ff` (보조 강조색)
- 배경: `#0a0e27`, `#1a1a2e`, `#16213e`

## 주의사항

- 첫 실행 시 Tesseract.js 언어 파일 다운로드로 인해 초기 로딩 시간이 있을 수 있음
- 이미지 품질이 낮으면 OCR 정확도가 떨어질 수 있음
- 한글 인식률은 폰트와 이미지 품질에 따라 다름
- 브라우저 콘솔에서 추출된 전체 텍스트를 확인하여 패턴 개선 가능
